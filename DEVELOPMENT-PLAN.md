# AI 评估系统 — 业余开发计划 v3

> **核心哲学**: AI 坐主桌，规则引擎当镜子。LLM 先打分，规则引擎并行跑来做对比。
> **模式**: 晚上 2-3 小时 / 周末半天 4 小时
> **v3 变化**: 从"赶 42 个 session 发布 v1.0.0"转为"AI 工程化能力建设"——每个模块交付一项可迁移的 AI 工程能力，不追求速度，追求深度。

---

## 执行状态

> 上次更新: 2026-07-21 | 当前: **A1 Prompt 工程体系**

### 已完成（M0-M3）

| 里程碑 | 内容 | Session | 状态 | 完成度 |
|--------|------|---------|------|--------|
| M0 | 地基 | S1-S2 | ✅ | 2/2 |
| M1 | ★ AI 先打分 | S3-S10 | ✅ | 8/8 |
| M2 | 规则引擎 + 对比 | S11-S18 | ✅ | 8/8 |
| M3 | 完整系统 | S19-S30 | ✅ | 12/12 (S20/S26 跳过) |

### 进行中（AI 工程化能力建设）

| 模块 | 主题 | 预计周期 | 状态 | 核心能力 |
|------|------|---------|------|---------|
| A1 | Prompt 工程体系 | 3-4 周 | 🔄 | Prompt 是可测试、可迭代的工程资产 |
| A2 | LLM 可观测性 | 2 周 | ⬜ | 每次 LLM 调用可见、可度量、可审计 |
| A3 | RAG 入门 | 3-4 周 | 🔄 | Embedding / 向量检索 / 检索质量度量 (A3.1+A3.2 ✅, A3.3 ⬜) |
| A4 | AI 可靠性工程 | 2 周 | ⬜ | 熔断/降级/重试/多模型 fallback 链 |
| A5 | 发布 | 2 周 | ⬜ | 能力沉淀后的输出，不是赶工交付 |

> 状态图例: ⬜ 未开始 | 🔄 进行中 | ✅ 已完成

---

## 为什么换路线？

v2 计划是"42 个 session → 3 个月 → v1.0.0"。实际跑完 M0-M3 后发现几个问题：

1. **业余 session ≠ 工作 session**。晚上 2-3 小时的有效编码时间约 1-1.5 小时（需要切上下文、回顾上次进度）。计划里写 3h 的 session，实际要 4-6h。
2. **残留的 12 个 session 塞了太多东西**。多模型对比、NL→JEXL、RAG 检索——每个都是独立的学习课题，3 小时完全不够做深。
3. **"赶 checkmark"和"提能力"是两件事**。42 个 session 赶完，你会得到一个可以展示的项目，但每个 AI 工程化能力都是浅的——跑通了，不理解。
4. **你的真实需求不是"发布一个开源项目"**。你是 Java 架构师往 AI 方向转，需要的是**可迁移的 AI 工程能力**——不是多一个 GitHub repo，是多一项简历上能写深度的技能。

v3 的核心变化：

```
v2:  S31→S32→...→S42 → v1.0.0 (赶 checkmark)
v3:  A1(Prompt工程) → A2(可观测性) → A3(RAG) → A4(可靠性) → A5(发布)

     每个模块做完，你在这件事上就是"做过的人"，不是"看过教程的人"。
```

---

## AI 工程化能力地图

> 这是评估系统能帮你建立的 AI 工程化能力。做完一个模块，对应的能力就是你的。

| 能力维度 | 对应模块 | 你能在简历/面试中说的 |
|---------|---------|---------------------|
| **Prompt 工程** | A1 | "我设计过多模型 prompt 对比框架，能系统性评估 prompt 效果，不是凭感觉调 prompt" |
| **LLM 可观测性** | A2 | "我给 LLM 调用加了分布式追踪——token 消耗、延迟分布、异常模式都有数据支撑" |
| **RAG 基础** | A3 | "我从零搭建过 embedding + 向量检索 pipeline，理解 chunking 策略对召回率的影响" |
| **AI 可靠性** | A4 | "我把后端可靠性模式（熔断/降级/重试）迁移到了 AI 调用链路上" |
| **AI 系统设计** | A1-A4 | "我设计过 AI 原生系统的评估闭环——LLM 打分 + 规则引擎验证 + 可观测性" |

---

## A1: Prompt 工程体系（3-4 周）🔄

> 核心认知: Prompt 不是"写一段话"。Prompt 是代码——可测试、可版本化、可对比、可回滚。

### A1.1 多模型对比框架
**时间**: 2-3 个 session（~8h）

```
□ 整合 2-3 个模型 provider (DeepSeek + 至少 1 个备选)
  - 统一 LlmClient 接口: chat(prompt, model) → LlmResponse
  - 每个 provider 的差异封装在适配层
□ 同一评估任务 → N 个模型各自打分
□ 对比维度:
  - 打分一致性: 同一指标不同模型差多少？
  - 评分稳定性: 同一 prompt 跑 3 次，方差多大？
  - 与规则引擎基准的偏差
□ API: POST /api/v1/ai-experiments/model-compare
  - 返回: { indexCode, modelScores: { deepseek: 85, glm: 72, qwen: 80 }, ruleScore: 70, variance: ... }
```

**验证**: 3 个模型 × 3 个指标 × 3 次重复 → 有统计学意义的对比数据

**AI 工程化收获**: 
- 多模型差异的直观感受
- 知道"一致性"和"稳定性"是两个不同维度
- 能说出 DeepSeek/GLM/Qwen 在不同指标类型上的表现差异

### A1.2 Prompt 版本化管理
**时间**: 2 个 session（~6h）

```
□ PromptTemplate 版本化:
  - 每个 prompt 有 version + changelog
  - 数据库/文件存储，可追溯
  - 评估记录关联 promptVersion → 事后可复盘
□ Prompt 效果评估:
  - 对同一个指标，用 3 个不同版本的 prompt 打分
  - 对比: 哪个版本更接近规则引擎基准？哪个更稳定？
  - eval_ai_experiment.prompt_version 字段
□ API: GET /api/v1/ai-experiments/prompt-history?indexCode=xxx
  - 返回: [{ version, avgScore, avgDiffFromRule, sampleCount, createdAt }]
```

**验证**: 同一个指标 → 2 个 prompt 版本 → 效果数据可对比

**AI 工程化收获**:
- Prompt 迭代不再靠记忆和直觉
- 能回答"v2 比 v1 改进了什么"而不是"我感觉改好了"
- 这套方法论可以迁移到任何 LLM 应用

### A1.3 自动评测 + 数据积累
**时间**: 2 个 session（~6h）

```
□ 跑 50 次评估（手动编不同场景的数据）
□ 整理 Prompt 实验日志:
  - 哪些指标 LLM 和规则引擎分歧最大？
  - 不同 prompt 版本对分歧的影响？
  - 不同模型在"需要上下文理解"vs"纯数值判断"的指标上差异？
□ 输出: 一份 Prompt 工程实验报告（不是你给别人的，是你自己的认知沉淀）
  - 写到 wiki/research/ 或 eval-system/docs/
  - 格式不限，但要能回答"你从这组实验里学到了什么"
```

**验证**: 50+ 条实验数据，有明确的实验结论

**核心交付物（A1 完成标志）**:
- [ ] 多模型对比有数据
- [ ] Prompt 版本可追溯
- [ ] 一份实验报告（你的认知，不是模板）

---

## A2: LLM 可观测性（2 周）⬜

> 核心认知: 后端有 APM（应用性能监控），LLM 调用也应该有。不观测的 LLM 就是黑盒。

### A2.1 调用追踪基础设施
**时间**: 2 个 session（~6h）

```
□ 每次 LLM 调用自动记录:
  - model: 用了哪个模型
  - promptVersion: 用了哪个版本的 prompt
  - inputTokens / outputTokens: token 消耗
  - durationMs: 调用耗时
  - retryCount: 重试次数
  - errorType: 错误类型（超时/限流/格式错误等）
  - llmScore → ruleScore diff: 与规则引擎的偏差（A1 已有）
□ eval_ai_experiment 表增强（增字段，不重建）
□ 用 AOP/拦截器自动采集，业务代码零侵入
```

**验证**: 跑一次评估 → 自动生成一条完整的调用记录

**AI 工程化收获**:
- 你的 Java AOP 经验直接迁移到 AI 场景
- 这是简历上能写的东西："设计了 LLM 调用的全链路追踪方案"

### A2.2 可观测性看板
**时间**: 2 个 session（~6h）

```
□ API: GET /api/v1/ai-experiments/stats
  - 按模型分组: 平均延迟 / P95延迟 / 平均token消耗 / 错误率
  - 按指标分组: 哪种指标 LLM 分歧最大？
  - 时间趋势: token 消耗、延迟的变化趋势
□ API: GET /api/v1/ai-experiments/anomalies
  - 异常检测: 单次 token 消耗 > 均值 3σ → 标记
  - 异常检测: 延迟突增 → 标记
□ 不做复杂 UI，API 返回 JSON 即可
□ 这个 API 的返回数据就是你写技术博客的素材
```

**验证**: stats API 返回有意义的统计数据，能回答"我的 LLM 调用健康吗？"

**AI 工程化收获**:
- LLM 调用从"玄学"变成"工程"
- 知道 OpenAI/DeepSeek API 的真实延迟分布
- 能评估"换个模型省多少成本"

**核心交付物（A2 完成标志）**:
- [ ] 每次 LLM 调用自动追踪
- [ ] stats + anomalies API 返回有意义的数据
- [ ] 能回答"我的 LLM 调用现在健康吗？"

---

## A3: RAG 入门（3-4 周）🔄

> 核心认知: RAG 不是"接个向量数据库"。RAG 是一整条 pipeline——embedding 怎么选、chunk 怎么切、检索怎么评、如何注入 prompt——每一步都有工程决策。

### A3.1 Embedding + 向量存储 ✅ (2026-07-21)
**实际耗时**: 1 个 session（~4h 编码 + 踩坑）

```
✅ 选型评估:
  - Embedding 模型: bge-small-zh-v1.5 (BAAI, 512维, ONNX 90MB, 本地 CPU 推理)
  - 向量存储: Lucene HNSW (纯 Java, 零外部依赖)
  - 决策依据: 本地部署/数据不出域/零 API 费用/Windows 兼容
  - 详细选型: wiki/ai/RAG-基础知识-从零到懂.md
✅ 历史评估记录 → Embedding → 向量存储:
  - 对指标名+实际值+LLM打分理由做 embedding
  - 启动时异步迁移 126 条历史记录（37秒）
  - 每次评估完成后增量写入索引
✅ 实现:
  - EmbeddingService: ONNX Runtime 直连推理, mean pooling + L2 归一化
  - VectorIndexService: Lucene 9.x KnnVectorField HNSW 索引
  - VectorRagService: encode(3-5ms) + search(6-11ms), 总延迟 ~10-15ms
  - RagIndexInitializer: 异步全量迁移 + 增量钩子
  - LlmScoringStrategy: 向量优先 → 规则降级 → 空 safe fallback 三层防护
✅ 落地指南: wiki/ai/RAG-落地实践指南.md
```

**验证**: ✅ 126 条历史记录 → embedding → 向量检索可用, 端到端评估通过

**坑点记录**:
- Maven 阿里云镜像劫持: `mirrorOf` 从 `*` 改为 `*,!central`
- DJL `djl-core` artifact 不存在 → 改用 `ai.djl:api`
- DJL `ModelZoo.loadModel()` 不支持本地 ONNX → 换 ONNX Runtime Java API 直连
- ONNX 模型需 `token_type_ids` 输入（代码只传了 input_ids + attention_mask）
- `Lucene99Codec` 类在 Lucene 9.12 中已移除

### A3.2 检索 + 注入 Prompt ✅ (2026-07-21)

```
✅ 检索策略:
  - 对每个指标分别向量检索 Top-2, 合并去重取 Top-3
  - 向量无结果 → 降级到 SimilarCaseService 规则检索
✅ 注入 LLM Prompt 作为 few-shot:
  - "## 语义相似历史案例（向量检索）" 段落
  - 案例含: 指标名 + 得分 + 理由 + 相似度%
✅ 已集成进 LlmScoringStrategy.buildFewShot() → H3 Pipeline
```

**验证**: ✅ 评估对象 → 自动向量检索 3 个相似案例 → 注入 prompt → LLM 打分参考

### A3.3 检索质量评测 ⬜
**时间**: 2 个 session（~6h）

```
□ 影子模式验证（建议先跑 1-2 周收集对比数据）:
  - 双跑 RULE + VECTOR, 记录到 eval_ai_experiment
  - 对比 Hit Rate@K 和 NDCG@K
□ 人工标注: 10-20 条"理想检索结果"
□ 输出: 一份 RAG 实验笔记（wiki/research/）
```

**核心交付物（A3 完成标志）**:
- [x] RAG pipeline 端到端跑通
- [ ] 检索质量有量化数据
- [ ] 一份 RAG 实验笔记

---

## A4: AI 可靠性工程（2 周）⬜

> 核心认知: 你的 Java 架构经验在这里直接变现。熔断/降级/重试/缓存——全是后端基础，但套在 LLM 调用上就是 AI 工程化。

### A4.1 多模型 Fallback 链
**时间**: 2 个 session（~6h）

```
□ 当前已有: LlmScoringStrategy 调用失败 → 默认 70 分（已实现 DEGRADED 标记）
□ 升级为: 主模型超时 → 自动切备选模型
  例: DeepSeek 超时 5s → 切 GLM → 切 Qwen → 才降级到默认分
□ fallback 链可配置（application.yml）:
  llm.fallback-chain: [deepseek, glm, qwen]
□ 每次 fallback 记录原因 + 耗时
```

**验证**: 故意让 DeepSeek 超时 → 自动切备选 → 正常返回

**AI 工程化收获**:
- 直接把 Java 微服务治理经验变现
- 理解 LLM 的不可靠性需要工程手段兜底

### A4.2 系统化熔断 + 降级
**时间**: 2 个 session（~6h）

```
□ 当前已有: AiCallGuard 连续5次失败熔断60s (S24)
□ 增强:
  - 熔断状态可视化 API
  - 半开状态自动探测
  - token 预算保护: 单次评估 token 消耗 > 阈值 → 不调 LLM，走规则引擎
□ 降级策略分层:
  Level 1: 切备选模型
  Level 2: 用规则引擎分数代替 LLM 分数
  Level 3: 返回默认 70 分 + DEGRADED 标记
□ eval_ai_experiment.degradation_level 记录降级层级
```

**验证**: 触发熔断 → 自动降级 → 恢复后自动半开探测 → 恢复

**AI 工程化收获**:
- 后端可靠性模式在 AI 场景的落地经验
- 这是面试中能展开讲 10 分钟的话题

**核心交付物（A4 完成标志）**:
- [ ] 多模型 fallback 链可配置、可观测
- [ ] 熔断/降级分层，有 API 可查询状态
- [ ] 降级路径有数据支撑

---

## A5: 发布（2 周）⬜

> 核心认知: 发布不是"代码扔上去"，是能力沉淀后的输出。你发布的不只是一个系统，是一套 AI 工程化实践。

### A5.1 技术博客
**时间**: 3 个 session（~9h）

```
□ 不是一篇，是一个系列（每篇聚焦一个 A 模块的实践）:
  1. "LLM 打分 vs 规则引擎：一个 Java 评估系统的 AI 化实践"（整体架构）
  2. "Prompt 工程不是玄学：多模型对比 + 版本化 + 效果评测"（A1）
  3. "让 LLM 调用可见：一个后端程序员的 AI 可观测性实践"（A2）
  4. "RAG 入门：从 embedding 到检索质量评测"（A3，选写）
□ 发布: 掘金 + 知乎
```

### A5.2 开源准备 + 发布
**时间**: 2 个 session（~6h）

```
□ LICENSE (Apache 2.0)
□ README 重写: 重点是架构设计思路 + AI 工程化理念，不是 feature list
□ 架构图 + 核心截图（A1-A4 积累的数据就是最好的截图素材）
□ Demo 场景: 1 个完整模型 + 2 个方案 + 50 个对象
□ Gitee/GitHub 推送
□ Git tag: v1.0.0
```

**核心交付物（A5 完成标志）**:
- [ ] 至少 1 篇技术博客发布
- [ ] Gitee/GitHub 仓库 Ready
- [ ] README 能体现 AI 工程化深度

---

## 附录：可选探索项（不排工期）

### NL → JEXL：用自然语言写规则表达式

**是什么**：业务人员用自然语言描述一条规则，LLM 自动翻译成 JEXL 表达式。

```
输入（自然语言）: "物流费用偏差超过20%就是红线"
输出（JEXL）:      attrValues["costDeviation"] > 20

输入: "入职天数小于30天的新员工，合格线降到60分"
输出: attrValues["daysSinceHire"] < 30 ? 60 : standardScore
```

**为什么放附录**：它是一个 **code generation** 任务，不是 AI 工程化基础能力。先理解 Prompt 工程、可观测性、RAG 之后，回头看这个会有更准确的判断——哪些能做、哪些难做、为什么难做。

**如果想探索**（不设时间表）：
- 先手写 10 条自然语言规则 → 让 LLM 试试转换 → 看准确率
- 理解 JEXL 沙箱安全：生成的表达式不能随意执行（`RuntimeException` 要在沙箱里跑）
- 核心难点不在"翻译"，在"变量名映射"——LLM 怎么知道 `costDeviation` 对应哪个字段？

**参考方向**：Text-to-SQL、GitHub Copilot 的代码补全——本质都是 NL → 结构化语言，工程上已经成熟，但领域特定（NL→JEXL）需要自己探索。

---

## 架构哲学（不变）

```
┌──────────────────────────────────────────────────┐
│              评估配置 (领域模型 — 不变)              │
│   模型 → Stage树 → 指标 → 参考标准                 │
│   (这套抽象是 AI 和规则引擎的共同底座)               │
├──────────────────────────────────────────────────┤
│            评分通道 (默认并行跑)                     │
│                                                   │
│   ┌─────────────┐          ┌─────────────┐       │
│   │  LLM 通道    │          │ 规则引擎通道  │       │
│   │ (默认)       │          │ (对比基线)    │       │
│   │ 语义判断     │    vs    │ JEXL 确定计算 │       │
│   │ 上下文感知   │          │ 可审计路径    │       │
│   └──────┬──────┘          └──────┬──────┘       │
│          │                        │               │
│          └───────────┬────────────┘               │
│                      │                            │
│              ┌───────▼────────┐                   │
│              │  对比 + 仲裁    │                   │
│              │  差异 > 阈值?   │                   │
│              └───────┬────────┘                   │
│                      │                            │
│              ┌───────▼────────┐                   │
│              │  树聚合 (规则引擎，永不替代) │       │
│              │  weighted_sum / sum / min  │       │
│              │  确定性的数学运算            │       │
│              └───────┬────────┘                   │
│                      │                            │
│              ┌───────▼────────┐                   │
│              │ 事件 + 等级 + 落库 │                 │
│              │ (LLM 异常检测 + 规则兜底) │          │
│              └────────────────┘                   │
└──────────────────────────────────────────────────┘

分工铁律:
  LLM 做: 语义判断 (打分、异常检测、总结)
  规则引擎做: 确定性运算 (聚合、排名、等级区间)
  永不混淆: 不让 LLM 做数学，不让规则引擎做判断
```

---

## AI 替代了什么？（一张表说清楚）

| Handler | AI 替代? | 为什么 |
|---------|---------|--------|
| H1 加载配置 | ❌ | 读数据库，AI 没价值 |
| H2 提取指标值 | ❌ | 字段映射，确定性操作 |
| **H3 标准匹配+打分** | **★ LLM-as-Judge (默认)** | 语义理解，上下文判断 |
| H3 派生指标 | ❌ | 数学运算，LLM 会算错 |
| **H3 树聚合** | **❌ 永不替代** | 加权求和必须确定，这是审计底线 |
| **H4 事件/红线** | **★ LLM 语义检测 (默认)** | 异常不靠穷举规则 |
| H5 申诉改分 | ❌ | 人工决策，系统只执行 |
| H6 等级映射 | ❌ | 分数→等级是查表 |
| **H6 总结** | **★ LLM 多轮对话** | 生成+自审 |
| Ranking | ❌ | 数学排序 |

**三个 AI 切入点，恰好是评估系统里最有判断力的三个环节。**

---

## 时间估算（v3 现实版）

| 模块 | 主题 | session 数 | 业余 ≈ | 关键交付 |
|------|------|-----------|--------|---------|
| A1 | Prompt 工程体系 | 6-7 | 3-4 周 | 多模型对比数据 + Prompt 版本化 + 实验报告 |
| A2 | LLM 可观测性 | 4 | 2 周 | 全链路追踪 + 统计 API |
| A3 | RAG 入门 | 7 | 3-4 周 | RAG pipeline ✅ + 检索质量评测 + 实验笔记 |
| A4 | AI 可靠性工程 | 4 | 2 周 | 多模型 fallback + 熔断降级分层 |
| A5 | 发布 | 5 | 2 周 | 技术博客 + 开源仓库 |
| **总计** | | **~27** | **~3-4 个月** | |

> 对比 v2: 12 个 session → 3 个月业余，现在 27 个 session → 3-4 个月。
> 时间长了，但做完你在这 5 个维度的 AI 工程化能力是实的——不是"我跑通了"而是"我做过、我理解、我能讲"。

---

## 关键提醒

1. **不做 NL→JEXL**。它是个 research problem，不是 AI 工程化能力训练场。投入产出比太低。

2. **每个模块必须有实验笔记**。不是代码跑通了就算完——你从实验数据里学到了什么？写到 wiki/research/ 下。这是你半年后复习时最值钱的东西。

3. **对比数据是你最值钱的资产**。A1 的多模型对比 + A3 的检索评测——这两组数据在任何 AI 项目上都是硬通货。

4. **树聚合永不交给 LLM**。审计底线，不变。

5. **发布是能力的副产品**。不是"为了发布而赶工"，而是"能力到了，发布是水到渠成"。v1.0.0 什么时候发，取决于 A1-A4 什么时候做完，不是日历上的日期。

6. **允许中断，不允许敷衍**。出差一周、加班三天、孩子生病——都正常。回来继续就行。但每个 session 的代码不能为了"赶上进度"而写烂。
